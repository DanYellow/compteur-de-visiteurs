{% extends "layouts/page.njk" %}

{% block title %}
Liste des membres
{% endblock %}
{% block scripts %}
    <script type="module" src="/src/assets/scripts/members-poll.ts" defer></script>
{% endblock %}

{% block content %}
    <div data-notifications-container class="fixed left-2 top-2 z-50 empty:h-0"></div>
    <div class="mt-8 px-3 grid grid-cols-1 xl:grid-cols-[220px_auto] gap-6">
        <aside>
            <nav>
                <header class="mb-6 flex justify-center mt-10">
                    <img src="/images/faclab-logo-light.svg" class="w-20" alt="">
                </header>

                <ul>
                    <li>
                        <a href="/visiteurs" class="title-font text-3xl block py-2 px-5 text-green-numixs">Visites</a>
                    </li>
                </ul>
            </nav>
        </aside>
        <div class="bg-slate-50 rounded-md py-5 px-6 md:py-10 md:px-13">
            <header class="flex flex-col md:flex-row justify-between md:items-center mb-16">
                <h1 class="text-black title-font font-bold text-6xl">Visites</h1>
                <a href="/visiteurs/telecharger" download class="justify-center inline-flex flex-row gap-2 bg-green-700 px-3.5 py-2.5 rounded-xl text-white hocus:bg-green-900">
                    Télécharger (.csv)
                    {% include "includes/download-fill-icon.svg" %}
                </a>
            </header>
            <div class="text-black">
                <header class="flex gap-4 mb-6 justify-between sm:justify-normal">
                    <a href="" class="py-2.5 px-3.5 border-black-numixs border rounded-md flex items-center justify-center">
                        <span class="flex items-center justify-center w-3.5">
                            {% include "includes/arrow-icon.svg" %}
                        </span>
                    </a>
                    <button class="py-2.5 px-5 border-black-numixs border rounded-md relative" commandfor="current-date" command="--open-date-picker">
                        <span class="pointer-events-none">{{ "Aujourd'hui" if is_today else current_date|date("EEEE dd LLLL yyyy")  }}</span>
                        <form action="" method="get">
                            {% set tomorrow = today|compute_days({func: "plus", days: 1})|date("yyyy-LL-dd") %}
                            <input type="date" name="current_date" id="current-date" max="{{ tomorrow }}" tabindex="-1" class="absolute inset-0 opacity-0 pointer-events-none">
                        </form>
                    </button>
                    <a href="" class="py-2.5 px-3.5 border-black-numixs border rounded-md flex items-center justify-center">
                        <span class="rotate-180 flex items-center justify-center w-3.5">
                            {% include "includes/arrow-icon.svg" %}
                        </span>
                    </a>
                </header>
                <table class="w-full bg-white border-collapse">
                    <caption class="text-start text-lg">
                        {{ visitors_list|length }} visite(s)
                    </caption>
                    <thead>
                        <tr class="bg-gray-200">
                            <td class="font-bold px-4 py-3 text-left">#</td>
                            <td class="font-bold px-4 py-3 text-left">Groupe(s)</td>
                            {# {% for business in list_business_sector %}
                                <td class="font-bold px-4 py-3 text-left border-solid">{{ business.name }}</td>
                            {% endfor %} #}
                            <td class="font-bold px-4 py-3 text-left">Date de visite</td>
                        </tr>
                    </thead>
                    <tbody>
                        {% for member in visitors_list %}
                            <tr class="even:bg-green-numixs/50 group hover:bg-slate-200" data-member-row>
                                <td class="group-hover:bg-slate-200 bg-gray-100 group-even:bg-green-numixs/50 px-4 py-3 text-left" data-label="Numéro" data-row-key="{{ business.value }}">{{ loop.index }}</td>
                                {# {% for business in list_business_sector %}
                                    <td class="group-hover:bg-slate-200 bg-gray-100 group-even:bg-green-numixs/50 px-4 py-3 text-left" data-label="{{ business.name }}" data-row-key="{{ business.value }}">{{ member[business.value] }}</td>
                                {% endfor %} #}
                                <td class="px-4 py-3 text-left" data-label="Date de passage" data-row-key="Date de passage">
                                    <time datetime="{{ member['date_passage'] }}">
                                        {{ member["date_passage"]|date("dd/LL/yyyy") }}
                                    </time>
                                </td>
                            </tr>
                        {% else %}
                            <tr class="">
                                <td colspan="15" class="px-4 py-3 text-center">Pas de visites le {{ current_date|date("dd/LL/yyyy") }}</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                    {# <tfoot>
                        <tr class="bg-gray-200">
                            <td class="font-bold px-4 py-3 text-left">#</td>
                            {% for business in list_business_sector %}
                                <td class="font-bold px-4 py-3 text-left border-solid">{{ business.name }}</td>
                            {% endfor %}
                            <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Date d'inscription</td>
                        </tr>
                    </tfoot> #}
                </table>
            </div>
        </div>
    </div>

    <template data-template-id="new-member-notification">
        <div class="flex items-center w-full max-w-xs p-4 mb-4 text-gray-500 bg-white rounded-lg shadow-sm notification" data-notification role="alert">
            <div class="inline-flex items-center justify-center shrink-0 w-8 h-8 text-green-500 bg-green-100 rounded-lg">
                <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5Zm3.707 8.207-4 4a1 1 0 0 1-1.414 0l-2-2a1 1 0 0 1 1.414-1.414L9 10.586l3.293-3.293a1 1 0 0 1 1.414 1.414Z"/>
                </svg>
                <span class="sr-only">Check icon</span>
            </div>
            <p class="ms-3 text-sm font-normal pr-1.5" data-notification-text>Item moved successfully.</p>
            <button type="button" class="ms-auto -mx-1.5 -my-1.5 bg-white text-gray-400 rounded-lg focus:ring-2 focus:ring-gray-300 p-1.5 hover:bg-gray-100 inline-flex items-center justify-center h-8 w-8" data-dismiss-notification-btn aria-label="Close">
                <span class="sr-only">Close</span>
                <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                </svg>
            </button>
        </div>
    </template>

{% endblock %}
