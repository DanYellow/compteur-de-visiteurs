{% extends "layouts/page.njk" %}

{% block title %}
Liste des membres
{% endblock %}
{% block scripts %}
    <script type="module" src="/src/assets/scripts/members-poll.ts" defer></script>
{% endblock %}

{% block content %}
    <div data-notifications-container class="fixed left-2 top-2 z-50 empty:h-0"></div>
    <header class="text-center mb-10 max-w-2xl mx-auto px-3">
        <h1 class="text-5xl text-black-numixs font-bold title-font mb-2.5">
   Liste des membres du FacLab<sup>®</sup> numixs
</h1>
    </header>
    <hr class="bg-gray-input-border-numixs">
    <div class="mt-8 px-3 mb-5">
        <table class="w-full bg-white border-collapse">
            <caption class="text-start px-4 text-xl">
                Nombre de membres : <span data-nb-members>{{ members_list|length }}</span>
            </caption>
            <thead>
                <tr class="bg-gray-200">
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <th class="px-4 py-3 border-l border-r border-gray-500 bg-gray-300 text-lg" colspan="7" scope="colgroup">Secteur d'activité</th>
                    <td></td>
                </tr>
                <tr class="bg-gray-200">
                    <td class="font-bold px-4 py-3 text-left border-solid">Prénom</td>
                    <td class="font-bold px-4 py-3 text-left border-solid">Nom de famille</td>
                    <td class="font-bold px-4 py-3 text-left border-solid">Numéro de téléphone</td>
                    <td class="font-bold px-4 py-3 text-left border-solid">E-mail</td>
                    <td class="font-bold px-4 py-3 text-left border-solid">Adresse</td>
                    {# <td class="font-bold px-4 py-3 text-left border-solid">Ville</td> #}
                    <td class="font-bold px-4 py-3 text-left border-solid">Code postal</td>
                    <td class="font-bold px-4 py-3 text-left border-solid">Informations supplémentaires</td>
                    {% for business in list_business_sector %}
                        <td class="{{ 'border-l border-gray-500' if loop.index0 == 0 else '' }} {{ 'border-r border-gray-500' if loop.last else '' }} bg-gray-300 font-bold px-4 py-3 text-left border-solid">{{ business.name }}</td>
                    {% endfor %}
                    <td class="font-bold px-4 py-3 text-left border-solid">Date d'inscription</td>
                </tr>
            </thead>
            <tbody>
                {% for member in members_list %}
                    <tr class="even:bg-green-numixs/50" data-member-row>
                        <td class="px-4 py-3 text-left" data-label="Prénom" data-row-key="prenom">{{ member.prenom }}</td>
                        <td class="px-4 py-3 text-left" data-label="Nom" data-row-key="nom">{{ member.nom }}</td>
                        <td class="px-4 py-3 text-left" data-label="Numéro de téléphone" data-row-key="numero_telephone">{{ member.numero_telephone|default("/", true) }}</td>
                        <td class="px-4 py-3 text-left" data-label="E-mail" data-row-key="email">{{ member.email }}</td>
                        <td class="px-4 py-3 text-left" data-label="Adresse" data-row-key="adresse">{{ member.adresse|default("/", true) }}</td>
                        {# <td class="px-4 py-3 text-left">{{ member.ville }}</td> #}
                        <td class="px-4 py-3 text-left" data-label="Code postal" data-row-key="code_postal">{{ member.code_postal }}</td>
                        <td class="px-4 py-3 text-left" data-label="Informations supplémentaires" data-row-key="extra_infos">{{ member.extra_infos|default("/", true) }}</td>
                        {% for business in list_business_sector %}
                            <td class="{{ 'border-l border-gray-500' if loop.index0 == 0 else '' }} {{ 'border-r border-gray-500' if loop.last else '' }} px-4 py-3 text-left bg-gray-100" data-label="{{ business.name }}" data-row-key="{{ business.value }}">{{ member[business.value] }}</td>
                        {% endfor %}
                        <td class="px-4 py-3 text-left" data-label="Date d'inscription" data-row-key="Date d'inscription">
                            <time datetime="{{ member['Date d\'inscription'] }}">
                                {{ member["Date d'inscription"] }}
                            </time>
                        </td>
                    </tr>
                {% else %}
                    <tr class="">
                        <td colspan="15" class="px-4 py-3 text-center">Pas de membres pour le moment</td>
                    </tr>
                {% endfor %}
            </tbody>
            <tfoot>
                <tr class="bg-gray-200">
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Prénom</td>
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Nom de famille</td>
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Numéro de téléphone</td>
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">E-mail</td>
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Adresse</td>
                    {# <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Ville</td> #}
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Code postal</td>
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Informations supplémentaires</td>
                    {% for business in list_business_sector %}
                        <td class="{{ 'border-l border-gray-500' if loop.index0 == 0 else '' }} {{ 'border-r border-gray-500' if loop.last else '' }} bg-gray-300 font-bold px-4 py-3 text-left border-solid">{{ business.name }}</td>
                    {% endfor %}
                    <td class="font-bold px-4 py-3 text-left border-solid border-slate-200">Date d'inscription</td>
                </tr>
            </tfoot>
        </table>
    </div>

    <template data-template-id="new-member-notification">
        <div class="flex items-center w-full max-w-xs p-4 mb-4 text-gray-500 bg-white rounded-lg shadow-sm notification" data-notification role="alert">
            <div class="inline-flex items-center justify-center shrink-0 w-8 h-8 text-green-500 bg-green-100 rounded-lg">
                <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5Zm3.707 8.207-4 4a1 1 0 0 1-1.414 0l-2-2a1 1 0 0 1 1.414-1.414L9 10.586l3.293-3.293a1 1 0 0 1 1.414 1.414Z"/>
                </svg>
                <span class="sr-only">Check icon</span>
            </div>
            <p class="ms-3 text-sm font-normal pr-1.5" data-notification-text>Item moved successfully.</p>
            <button type="button" class="ms-auto -mx-1.5 -my-1.5 bg-white text-gray-400 rounded-lg focus:ring-2 focus:ring-gray-300 p-1.5 hover:bg-gray-100 inline-flex items-center justify-center h-8 w-8" data-dismiss-notification-btn aria-label="Close">
                <span class="sr-only">Close</span>
                <svg class="w-3 h-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 14">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 6 6m0 0 6 6M7 7l6-6M7 7l-6 6"/>
                </svg>
            </button>
        </div>
    </template>

{% endblock %}
